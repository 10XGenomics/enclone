<title>fetching datasets for enclone</title>

<body>

<br>
<a href="../../index.html#help">
<img src="../../img/enclone_banner.png" alt="enclone banner" title="enclone banner" width=100% />
</a>

<h1>Fetching test datasets</h1>

<p>Here are three methods that should do the same thing.  First <code>cd</code> so that you're in your
home directory.  Then do <b>one</b> of the following:</p>
<ol>
<li>
<code>svn export https://github.com/10XGenomics/enclone/trunk enclone</code>
</li>
<li>
<code>curl -L https://github.com/10XGenomics/enclone/archive/master.tar.gz | tar zx enclone-master/test; mv enclone_master enclone</code>
</li>
<li>
<code>wget -O- https://github.com/10XGenomics/enclone/archive/master.tar.gz | tar zx enclone-master; mv enclone-master enclone</code>
</li>
</ol>

<h2>Installing curl</h2>

<p>If, perchance, neither <code>svn</code> nor <code>curl</code> nor <code>wget</code> are installed on your computer, you will need
to install one of them.  There are a few ways to do this depending on which flavor of Unix you use. Here are some ways to get <code>curl</code> onto your machine.</p>

<h3>Debian/Ubuntu</h3>

<p>For these machines, use <b>one</b> of the following:</p>
<ol>
<li>
<code>apt-get install curl</code>
</li>
<li>
<code>sudo apt-get install curl</code>
</li>
<li>
<code>sudo apt install curl</code>
</li>
</ol>

<h3>Mac OS X</h3>

<p>To get the latest <code>curl</code> on OS X, you will likely want to use the latest <a href="https://brew.sh" rel="nofollow">Homebrew</a>:</p>
<ol>
<li><code>/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"</code></li>
<li><code>brew install curl</code></li>
</ol>

<h3>CentOS/Fedora/RHEL</h3>

<p>For these machines, use the following:</p>
<p><code>yum install curl</code></p>

<h3>Other flavors</h3>

<p>If you're using another flavor of Unix, these may be helpful:</p>
<ol>
<li>
<b>OpenSUSE:</b> <code>zypper install curl</code>
</li>
<li>
<b>ArchLinux:</b> <code>pacman -Sy curl</code>
</li>
</ol>

<h1>Installing wget</h1>

<p>Have something against <code>curl</code> but don't know how to install <code>wget</code>? This section is for you.</p>

<h3>Debian/Ubuntu</h3>

<p>For these machines, use <b>one</b> of the following:</p>
<ol>
<li>
<code>apt-get install wget</code>
</li>
<li>
<code>sudo apt-get install wget</code>
</li>
<li>
<code>sudo apt install wget</code>
</li>
</ol>

<h3>Mac OS X</h3>

<p>To get the latest <code>wget</code> on OS X, you will likely want to use the latest <a href="https://brew.sh" rel="nofollow">Homebrew</a>:</p>
<ol>
<li><code>/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"</code></li>
<li><code>brew install wget</code></li>
</ol>

<h3>CentOS/Fedora/RHEL</h3>

<p>For these machines, use the following:</p>
<p><code>yum install wget</code></p>

<h3>Other flavors</h3>

<p>If you're using another flavor of Unix, these may be helpful:</p>
<ol>
<li>
<b>OpenSUSE:</b> <code>zypper install wget</code>
</li>
<li>
<b>ArchLinux:</b> <code>pacman -Sy wget</code>
</li>
</ol>

<h1>Installing svn</h1>

<p>If you'd rather use <code>svn</code> instead of <code>wget</code> or <code>curl</code>, feel free! If you don't know how to install <code>svn</code>, this section
should help you do so.</p>

<h3>Ubuntu</h3>

<p>For these machines, run the following commands <b>in order</b>:</p>
<ol>
<li>
<code>apt-get install subversion</code>
</li>
<li>
<code>apt-get install libapache2-svn</code>
</li>
</ol>

<h3>Debian</h3>

<p>For these machines, run the following commands <b>in order</b>:</p>
<ol>
<li>
<code>apt-get install subversion</code>
</li>
<li>
<code>apt-get install libapache2-mod-svn</code>
</li>
</ol>

<h3>Mac OS X</h3>

<p>To get the latest <code>svn</code> on OS X, you will likely want to use the latest <a href="https://brew.sh" rel="nofollow">Homebrew</a>:</p>
<ol>
<li><code>/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"</code></li>
<li><code>brew install subversion</code></li>
</ol>

<h3>Fedora</h3>

<p>For Fedora machines, run <code>yum install subversion</code>.</p>

<h3>CentOS/RHEL</h3>

<p>For these machines, run the following commands <b>in order</b>:</p>
<ol>
<li>
<code>yum install subversion</code>
</li>
<li>
<code>yum install mod_dav_svn</code>
</li>
</ol>

<h3>Other flavors</h3>

<p>If you're using another flavor of Unix, these may be helpful:</p>
<ol>
<li>
<b>OpenSUSE:</b> <code>zypper install subversion</code> and then <code>zypper install subversion-server</code>
</li>
<li>
<b>ArchLinux:</b> <code>pacman -Sy subversion</code>
</li>
</ol>

<h1>I still can't install curl, wget, or svn!</h1>

<p>If all else fails, maybe these instructions from xkcd will help:</p>

<img alt="universal install script" title="universal install script" src="https://imgs.xkcd.com/comics/universal_install_script.png" style="max-width:100%;"></a>

<p>Well seriously, if you're really stuck, please write to us at <a href="mailto:enclone@10xgenomics.com">enclone@10xgenomics.com</a>.</p>

</body>
</html>
