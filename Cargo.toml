[workspace]

members = [
    "enclone",
    "enclone_core",
    "enclone_help",
    "enclone_main",
    "enclone_print",
    "enclone_tail",
    "enclone_versions",
]

[profile.dev]
debug = 1
opt-level = 3

[profile.release]
debug = 1
lto = true
codegen-units = 1

[profile.test]
opt-level = 3
